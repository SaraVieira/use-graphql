export { default as theme } from './src'
import { Head, Image } from 'mdx-deck'
import { Layout, LayoutNoFooter, CodeSanbox, Split, BGImage } from './src'
import { CodeSurfer } from 'mdx-deck-code-surfer'
import nightOwl from 'prism-react-renderer/themes/nightOwl'

<Head>
  <title>useGraphQL()</title>
</Head>

# useGraphQL()

---

export default Layout

# I really love GraphQL

---

export default Layout

# My name is Sara

---

export default Split

<img src={require('file-loader!./images/sandbox.png')} />

- Developer at CodeSanbox
- Really into shitty movies
- Really into football

---

export default Layout

# I used to live in Porto

## I moved to Berlin

---

export default Layout

# The land of sun and amazing architecture

<BGImage src="https://lh3.googleusercontent.com/xsqTE3FnOlBLDBjKCLOxXYbO9SKbDnpZJbt3pcJoRxJe4WsiWU-56YRvfwRkqMnoq5jFdEEFBBtc4itu_F-oihyPxh-4-ek0c54BQdqjmFUUBCGQM8NA_53DnZhwU4o2_gL_VZd_e-ZKMB7nnmCcmOW-fHmh1sjbeRrBAsyI0e9kpBAvg1dV9xnUGDoI8U8p4YdwEzSSSWTKdw16vctfoitEjFMLHNZNCHQjxHcYI_YnGbRgq5xRsOh4vHOhnkpmBntkOVqV22SIeAVfCAwUGO-STovJ0VdOf2x8G3WIxwrAw0fh86uc648G9AhpgxiqSIyhYKmoR_mCNl8te-PtQFW0HiCEDFyh2UnMpyUA2aoStbwNODzWFC1-Lm2TE-JATPH18IOHLERfXiHlL7Dkw3j4x1ui3B83JJ1tdRLxa7IvvDWyH4J0mv-585DNuLi7gEZKtFYOkCeHL5B9-VG0OmcEN__-oz4o_c7ra9X6m-5yNETmxyPGC8n8wRcbISqtWJhBSjCG_g8tichibCS2MEGn7prdofupYz6vQRKQM1kZttLMY0PWG1NfIVakk6B-7CsOXQxSBBGvU2PpbGXLzZxGNcDAS3bTXlAtUmwinR0uPjFopXx-P6sUmPp4iyTyIxBNcELGl9jpHxS2fUxrkO003hBY7ZQ=w1888-h1416-no" />

---

export default Layout

# Gourmet Food

<BGImage src="https://berlinfoodstories.com/wp-content/uploads/2015/10/Doyum-Berlin-Best-Doner-.jpg" />

---

export default Layout

# Very Conservative and German

<BGImage src={require('file-loader!./images/dildoking.png')} />

---

export default Layout

# Land of the best airports

<BGImage src="https://www.airlinequality.com/wp-content/uploads/2018/04/IMG_7443-e1524598602700.jpg" />

---

export default Layout

# We are not here to talk about airports

---

export default Layout

# I wish

---

export default Layout

# What is GraphQL?

---

<Image src={require('file-loader!./images/graphql.png')} />

---

export default Layout

# A Query Language for your API

---

export default Layout

# Why Should I Care ?

---

export default Layout

# GraphQL APIs have a strongly typed schema

---

export default Layout

<CodeSurfer
  theme={nightOwl}
  code={`
import gql from 'graphql-tag'
\n
const Schema = gql\`
  type Speaker {
    id: ID!
    name: String
    photo: String
    talks: [Talk]
  }
\n
  type Talk {
    name: String
    speaker: Speaker
  }
\`
`}
/>

---

export default Layout

# It's framework agnostic

---

export default Layout

# And language too

---

export default Split

- Javascript
- Typescript
- Ruby
- PHP
- Python
- Java
- C/C++

* Perl
* .NET
* Erlang
* Elixir
* Haskell
* Rust
* Lua

---

export default Split

- Scala
- Clojure
- ClojureScript
- Swift
- OCaml
- SQL

* R
* Elm
* Julia
* Kotlin
* Crystal
* Go

---

export default Layout

# You get the Point

---

export default Layout

# Automatic Documentation

---

export default Layout

Since you have to type all your API graphql uses that to create automatic documentation for your API

---

export default Layout

# UI Editors

---

export default LayoutNoFooter

# GraphQL Playground

<BGImage src="https://camo.githubusercontent.com/1a26385e3543849c561cfafd0c25de791a635570/68747470733a2f2f692e696d6775722e636f6d2f41453557364f572e706e67" />

---

export default LayoutNoFooter

# GraphiQL

<BGImage src="https://github.com/graphql/graphiql/raw/master/resources/graphiql.png" />

---

export default Layout

# You get what you asked for

---

export default Layout

# [Open on CodeSanbox](https://mq61mzo28p.codesandbox.io/)

---

export default Layout

# The Frontend is Love

---

export default Layout

Tools like Apollo handle all the loading and error states for you.

---

export default Layout

Also the Caching. And we hate caching.

---

export default Layout

# All The Frameworks

---

<CodeSurfer
  theme={nightOwl}
  language="markup"
  code={`
<ApolloQuery :query="require('../graphql/query.gql')">
  <template slot-scope="{ result: { loading, error, data } }">
    <div v-if="loading" class="loading apollo">Loading...</div>
\n
    <div v-else-if="error" class="error apollo">An error occured</div>
\n
    <div v-else-if="data" class="result apollo">{{ data.hello }}</div>
\n
    <div v-else class="no-result apollo">No result :(</div>
  </template>
</ApolloQuery>
`}
/>

---

<CodeSurfer
  theme={nightOwl}
  language="javascript"
  code={`
class ProfileComponent implements OnInit {
  loading: boolean;
  currentUser: any;
\n
  private querySubscription: Subscription;
\n
  constructor(private apollo: Apollo) {}
\n
  ngOnInit() {
    this.querySubscription = this.apollo.watchQuery<any>({ query: Query})
    .valueChanges.subscribe(({ data, loading }) => {
        this.loading = loading;
        this.currentUser = data.currentUser;
    });
  }
}
`}
/>

---

<CodeSurfer
  theme={nightOwl}
  code={`
  <Query query={query}>
    {({ loading, error, data }) => {
      if (loading) return 'Loading...';
      if (error) return error.message;
\n
      return (
        {data.dogs.map(dog => (
          <li key={dog.id} value={dog.breed}>
            {dog.breed}
          </li>
        ))}
      );
    }}
  </Query>
`}
/>

---

export default Layout

# It's 2019, why u show render props?

---

export default Layout

# Wanna hook up some GraphQL?

---

export default Layout

# [Open on CodeSanbox](https://codesandbox.io/s/xv3039nvlw)

---

export default Layout

# There is no limit to what you can build

---

export default LayoutNoFooter

# Apollo Link State

<BGImage src={require('file-loader!./images/link-state.png')} />

---

export default LayoutNoFooter

# GraphQL CSS

<BGImage src={require('file-loader!./images/css.png')} />

---

export default LayoutNoFooter

<BGImage src={require('file-loader!./images/yay.jpg')} />

# Thank You 🇵🇹

### [https://usegraphql.netlify.com/](https://usegraphql.netlify.com/)
